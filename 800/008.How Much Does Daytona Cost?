#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
      int n,k,c=0;
      cin>>n>>k;
      int a[n];
      for(int i=0;i<n;i++)
      {
          cin>>a[i];
          if(k==a[i]){
            c++;
          }
      }
     if(c>=1){
             cout<<"Yes"<<endl;
     }
     else{
            cout<<"No"<<endl;
      }
    }
}
Note
    Simply count the frequency of 𝑘.If the occurrence of 𝑘 is more than 1, print it.
From the test case, only the 𝑘 element occurs more than one time.
